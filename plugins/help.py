# Define the help function to handle the /help command
def help(update, context):
    context.bot.send_message(chat_id=update.effective_chat.id, text="I Can't Do Anything Just Send Me Text I Will Make It Stylish.")
